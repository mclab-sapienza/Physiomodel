/* Asserts */
/* Simulation code for Test generated by the OpenModelica Compiler v1.9.6. */

#include "openmodelica.h"
#include "openmodelica_func.h"
#include "simulation_data.h"
#include "simulation/simulation_info_json.h"
#include "simulation/simulation_runtime.h"
#include "util/omc_error.h"
#include "simulation/solver/model_help.h"
#include "simulation/solver/delay.h"
#include "simulation/solver/linearSystem.h"
#include "simulation/solver/nonlinearSystem.h"
#include "simulation/solver/mixedSystem.h"

#include <string.h>

#include "Test_functions.h"
#include "Test_model.h"
#include "Test_literals.h"




#if defined(HPCOM) && !defined(_OPENMP)
  #error "HPCOM requires OpenMP or the results are wrong"
#endif
#if defined(_OPENMP)
  #include <omp.h>
#else
  /* dummy omp defines */
  #define omp_get_max_threads() 1
#endif
#if defined(__cplusplus)
extern "C" {
#endif


/*
 equation index: 22
 type: ALGORITHM
 
   assert(diastole.EDV >= 0.0, "Variable diastole.EDV out of [min, max] interval: diastole.EDV >= 0.0 has value: " + String(diastole.EDV, "g"));
 */
void Test_eqFunction_22(DATA *data, threadData_t *threadData)
{
  TRACE_PUSH
  const int equationIndexes[2] = {1,22};
  modelica_boolean tmp15;
  static const MMC_DEFSTRINGLIT(tmp16,81,"Variable diastole.EDV out of [min, max] interval: diastole.EDV >= 0.0 has value: ");
  modelica_string tmp17;
  static int tmp18 = 0;
  modelica_metatype tmpMeta[1] __attribute__((unused)) = {0};
  if(!tmp18)
  {
    tmp15 = GreaterEq($Pdiastole$PEDV,0.0);
    if(!tmp15)
    {
      tmp17 = modelica_real_to_modelica_string_format($Pdiastole$PEDV, (modelica_string) mmc_strings_len1[103]);
      tmpMeta[0] = stringAppend(MMC_REFSTRINGLIT(tmp16),tmp17);
      {
        FILE_INFO info = {"/Users/stefano/Repo/physiology/Physiomodel/Physiomodel/Build/CardioVascular/Heart/Diastole/Test.mo",6,3,6,46,0};
        omc_assert_warning(info, "The following assertion has been violated %sat time %f\ndiastole.EDV >= 0.0", initial() ? "during initialization " : "", data->localData[0]->timeValue);
        omc_assert_warning_withEquationIndexes(info, equationIndexes, MMC_STRINGDATA(tmpMeta[0]));
      }
      tmp18 = 1;
    }
  }
  TRACE_POP
}
/* function to check assert after a step is done */
int Test_checkForAsserts(DATA *data, threadData_t *threadData)
{
  TRACE_PUSH

  Test_eqFunction_22(data, threadData);
  
  TRACE_POP
  return 0;
}

#if defined(__cplusplus)
}
#endif

