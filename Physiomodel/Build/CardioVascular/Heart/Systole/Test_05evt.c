/* Events: Sample, Zero Crossings, Relations, Discrete Changes */
/* Simulation code for Test generated by the OpenModelica Compiler v1.9.6. */

#include "openmodelica.h"
#include "openmodelica_func.h"
#include "simulation_data.h"
#include "simulation/simulation_info_json.h"
#include "simulation/simulation_runtime.h"
#include "util/omc_error.h"
#include "simulation/solver/model_help.h"
#include "simulation/solver/delay.h"
#include "simulation/solver/linearSystem.h"
#include "simulation/solver/nonlinearSystem.h"
#include "simulation/solver/mixedSystem.h"

#include <string.h>

#include "Test_functions.h"
#include "Test_model.h"
#include "Test_literals.h"




#if defined(HPCOM) && !defined(_OPENMP)
  #error "HPCOM requires OpenMP or the results are wrong"
#endif
#if defined(_OPENMP)
  #include <omp.h>
#else
  /* dummy omp defines */
  #define omp_get_max_threads() 1
#endif
#if defined(__cplusplus)
extern "C" {
#endif

/* Initializes the raw time events of the simulation using the now
   calcualted parameters. */
void Test_function_initSample(DATA *data, threadData_t *threadData)
{
  long i=0;
}

const char *Test_zeroCrossingDescription(int i, int **out_EquationIndexes)
{
  static const char *res[] = {"time < step.startTime"};
  static const int occurEqs0[] = {1,11};
  static const int *occurEqs[] = {occurEqs0};
  *out_EquationIndexes = (int*) occurEqs[i];
  return res[i];
}

/* forwarded equations */

int Test_function_ZeroCrossingsEquations(DATA *data, threadData_t *threadData)
{
  TRACE_PUSH
  
  data->simulationInfo->callStatistics.functionZeroCrossingsEquations++;
  
  
  TRACE_POP
  return 0;
}

int Test_function_ZeroCrossings(DATA *data, threadData_t *threadData, double *gout)
{
  TRACE_PUSH
  modelica_boolean tmp1005;
  
  data->simulationInfo->callStatistics.functionZeroCrossings++;
  
  tmp1005 = LessZC(data->localData[0]->timeValue, $Pstep$PstartTime, data->simulationInfo->storedRelations[0]);
  gout[0] = (tmp1005) ? 1 : -1;
  
  TRACE_POP
  return 0;
}

const char *Test_relationDescription(int i)
{
  const char *res[] = {"time < step.startTime"};
  return res[i];
}

int Test_function_updateRelations(DATA *data, threadData_t *threadData, int evalforZeroCross)
{
  TRACE_PUSH
  modelica_boolean tmp1006;
  
  if(evalforZeroCross) {
    tmp1006 = LessZC(data->localData[0]->timeValue, $Pstep$PstartTime, data->simulationInfo->storedRelations[0]);
    data->simulationInfo->relations[0] = tmp1006;
  } else {
    data->simulationInfo->relations[0] = (data->localData[0]->timeValue < $Pstep$PstartTime);
  }
  
  TRACE_POP
  return 0;
}

int Test_checkForDiscreteChanges(DATA *data, threadData_t *threadData)
{
  TRACE_PUSH
  int needToIterate = 0;

  infoStreamPrint(LOG_EVENTS_V, 1, "check for discrete changes at time=%.12g", data->localData[0]->timeValue);
  if (ACTIVE_STREAM(LOG_EVENTS_V)) messageClose(LOG_EVENTS_V);
  
  TRACE_POP
  return needToIterate;
}

#if defined(__cplusplus)
}
#endif

